/*jshint node:true*/
module.exports = {
  description: '',
  // Prevent unnecessary errors since entity name is not required
  normalizeEntityName: function() {},

  // afterInstall: function(options) {
  //   // Perform extra work here.
  //   // Once addons are public, we could install ember-logging-service here
  // }
};
